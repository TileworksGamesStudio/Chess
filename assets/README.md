Replace board.svg, upper.svg, lower.svg, and every file under pieces/ with your own art.
Optional: add BACKGROUND.png for the full-screen page background.
Pieces use the builder-configured PIECE_SCALE value; the generated CSS receives that value directly.
The app intentionally contains no external board/piece artwork.
